"""LLM probability adjuster (Phase 3).

Adjusts the baseline `p` from Pillar A/B and returns an adjusted probability plus
a rationale string. The NUMBER, not the prose, drives the recommendation, and it
re-enters the EV gate — it never invents a recommendation with no quantitative
anchor. Stubbed in Phase 1.
"""

from __future__ import annotations


def adjust(*args, **kwargs):  # pragma: no cover - Phase 3
    raise NotImplementedError("adjuster is implemented in Phase 3.")
